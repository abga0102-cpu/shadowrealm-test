# Archived one-shot workflows

These workflows were used to apply historical game patches by rewriting repository files and, in some cases, committing/pushing the result.

They are intentionally stored outside `.github/workflows/` so GitHub Actions will not execute them.

The static HTML/CSS/JavaScript game remains unchanged by this archive move. Keep these files only as historical implementation references; new CI should validate source rather than mutate it.
